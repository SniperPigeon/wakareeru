# 数据标签待办

- [ ] 检查 `E257系` 与 `E257系-2000・2500番台` 的混杂；重点核对 LLM 提取的 `submodel` / `bandai`、文件名中的番台与重复 Commons 文件的跨标签一致性。
- [ ] 检查北越急行曾有的 `683系-8000番台` 能否从当前 `683系` 数据中可靠拆分；优先核对 `JR West 683-8000` Commons 分类路径、现有图片的 `category_path` / 文件名、LLM `bandai`，并确认转售 JR 西日本后的涂装变化是否需要单独规则。本项仅做可分性评估，暂不把特急车辆加入北越急行人工车型目录。
